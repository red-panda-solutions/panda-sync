name: panda_sync_todo
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  path_provider: ^2.1.3
  fluttertoast: ^8.2.5
  dio: 5.4.3+1
  json_serializable: ^6.8.0
  build_runner: ^2.4.9

  panda_sync:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  integration_test:
    sdk: flutter
  isar:
  dio:
  connectivity_plus:

flutter:
  uses-material-design: true